# Task Planner — роли и разграничение доступа (план работ)

> Создано **2026-08-04**. Задача: внутри Task Planner два вида пользователей — **админы
> планировщика** (создают задачи, гоняют AI-планирование) и **бригадиры/PM**, которым
> нужен сильно урезанный вид. Тут — принятая модель, что уже сделано и **полный список
> UI-работ**. Функционал бригадира заказчик даёт отдельно — под него оставлены слоты.
> Смежное: [`TASK-PLANNER.md`](TASK-PLANNER.md) (бизнес-логика, n8n), [`PORTAL.md`](PORTAL.md) (платформа).

---

## 1. Принятые решения (2026-08-04)

| # | Решение | Почему |
|---|---|---|
| **Р-1** | **Вход только через Google OAuth**, как у всех остальных | Все 9 строк `tp_teams` имеют email на `@achgroupllc.com` — это подтверждённый Workspace-домен портала (`PORTAL.md` §8). Отдельный вход бригадирам не нужен |
| **Р-2** | **Роль внутри апки не хранится**, а выводится из портальных данных | Правило 3 платформы: контур допуска — портальный. Апка не заводит свой |
| **Р-3** | `tp_user_roles` + кнопка **Set password** — **выпилены из UI** | Наследие Lovable-версии TP со своим логином. Кнопка вела в тупик: создавала учётку email+пароль, а на экране входа портала формы пароля нет вообще — войти такой учёткой невозможно |
| **Р-4** | **RLS на `tp_*` закрывается** (миграция `0006`) | Найдена утечка: часть таблиц читалась анонимом без логина (см. `PORTAL.md`, SEC-7) |
| **Р-5** (08-13) | **Исполнение — отдельная ось `execution_status`**, а не новые значения в `status` | `scheduled` и «выполнена» не взаимоисключающи: задача остаётся запланированной на дату и при этом выполнена. Подробно — §5 |
| **Р-6** (08-13) | **`in_progress` не вводим** | У бригадира нет кнопки «Start» — статус без явного действия врал бы. Вводить только вместе с кнопкой, если попросят |
| **Р-7** (08-13) | **История задачи — журнальная таблица `tp_task_events`**, а не поля в задаче | В одну ленту должны лечь отметка бригадира, комментарий апрувера и возврат на доработку (§5.2) |

---

## 2. Модель ролей

**Где настраивается:** `/settings/task-planner` → вкладка **Roles**. Админ отмечает, какие
**портальные роли** дают вид «Planner Admin», а какие — «Team Lead». Маппинг лежит в
`app_settings` под ключами `roles_admin` / `roles_team_lead` (jsonb-массив `role_id`).
Сами роли и пользователи заводятся только в Portal Settings — апка своего реестра не имеет
(правило 3). Слоты ролей объявлены в `appRegistry.ts` → `AppConfig.appRoles`, поэтому
вкладка Roles автоматически появится у любой другой апки, которой понадобятся свои виды.

Роль вычисляется на лету, нигде не хранится (`useTaskPlannerRole()`), по порядку:

| # | Условие | Роль | Кто это |
|---|---|---|---|
| 1 | админ портала (`useAuth().isAdmin`) | **`admin`** | Влад, Павел — всегда, независимо от настройки |
| 2 | роль юзера ∈ `roles_admin` (вкладка Roles) | **`admin`** | планировщик-диспетчер без прав админа портала |
| 3 | email юзера совпал с `tp_teams.email` | **`team_lead`** | бригадир/PM своей бригады |
| 4 | роль юзера ∈ `roles_team_lead` | **`team_lead`** | бригадир, которого нет в `tp_teams` (без привязки к бригаде) |
| 5 | остальные | **`none`** | **в апку не пускаем** — Access denied |

**Видов ровно два.** Доступ к апке в портале (`applications`) сам по себе вида не даёт: если
пользователю не назначен ни Planner Admin, ни Team Lead, `TaskPlannerLayout` показывает
Access denied вместо любого экрана планировщика. Гейт стоит на обёртке всех роутов
`/task-planner/*`, поэтому прямая ссылка его не обходит.

Реализация — два зеркальных слоя, гейт обязан быть на обоих:

- **UI**: `pages/task-planner/useTaskPlannerRole.ts` → `{ role, teamId, teamName }`.
  Ищет бригаду через `fetchTeamByEmail()` (узкий запрос по `tp_teams`, `ilike` = регистронезависимое равенство).
- **БД**: SQL-функция `public.tp_my_team_id()` (миграция `0006`, `security definer`) — тем же
  правилом (email ↔ `tp_teams.email`) скоупит RLS-политики.

Чтобы бригадир вообще попал в апку, ему нужно (обычным портальным путём, **не** внутри TP):
1. приглашение в **Portal Settings → Users** на рабочий Google-аккаунт;
2. роль, у которой в доступах есть Task Planner (`role_applications`);
3. эта же роль — отмечена в **App Settings → Roles → Team Lead** (либо его email есть в `tp_teams`).

---

## 3. Что уже сделано (2026-08-04)

- [x] **Вкладка Roles в App Settings** (`/settings/task-planner`) — маппинг «внутренняя роль
      апки → портальные роли», сохраняется в `app_settings`. Механизм общий: слоты объявляются
      в `appRegistry` (`appRoles`), вкладка появляется сама. Сервис — `getAppRoleIds()`.
- [x] `useTaskPlannerRole()` — хук роли (портальный админ / маппинг ролей / бригада по email).
- [x] `fetchTeamByEmail()` в `services/task-planner/data.ts`.
- [x] `tp_my_team_id()` + закрытие RLS — миграция `0006_tp_rls_lockdown.sql`, **применена на боевой** 2026-08-04.
- [x] Убраны кнопка **Set password**, модалка и сервис `setTeamPassword`; на вкладке Team —
      пояснение, что доступ выдаётся в Portal Settings.
- [x] Меню апки переименовано: **Admin → Directories** (не путать с админкой портала).
- [x] Портальное **User Management → Portal Settings** (`/portal-settings`, старый `/users` — редирект).

**Заход 2026-08-13 — исполнение задачи:**
- [x] Миграция **`0007_tp_task_execution.sql`** — применена на боевой 2026-08-13.
- [x] Миграция **`0008_tp_planner_admin.sql`** — «админ апки» в БД = вид Planner Admin
      (`tp_is_planner_admin()`), а не только админ портала. ⏳ на боевой не применена.
- [x] Сервис `services/task-planner/execution.ts` — все переходы через RPC, фото по signed URL.
- [x] Экран **My Tasks** (`/task-planner/my-tasks`) — день + Yesterday/Today/Tomorrow,
      Complete, Notes, Photos, окно истории.
- [x] Экран **Approvals** (`/task-planner/approvals`) — Approve / Send to rework (комментарий обязателен).
- [x] Общий механизм «пункт меню виден виду апки»: `AppNavItem.appRoles` + `AppRoleProvider`
      (`app/AppRoleContext.tsx`) + чистая `visibleNavItems()`, покрытая тестом
      `tests/app-nav-visibility.test.ts`. Им же воспользуются HR-Checklists и Production.
- [x] `PlannerAdminOnly` — гейт планировочных роутов по виду апки (портального `AdminOnly`
      мало: Planner Admin может быть выдан ролью, не будучи админом портала).

---

## 4. Что надо сделать по UI — полный список

### 4.1 Оболочка и навигация

- [x] **Пункты меню по роли** — сделано 2026-08-13 ровно так: апка публикует вид
      (`usePublishAppRole` в `TaskPlannerLayout`), `Layout` фильтрует через `visibleNavItems()`.
      Провайдер живёт НАД `Layout` (он рендерит `<Outlet/>`), значение пишется снизу вверх.
- [x] **Роут-гейт `/task-planner/admin`** — 🔴 был открыт по прямой ссылке любому, у кого есть
      доступ к Task Planner (роут без `AdminOnly`; `AppAccessGuard` различает только апки
      целиком, а `TaskPlannerLayout` ничего не проверял). Закрыт `AdminOnly` 2026-08-04.
- [x] Тем же гейтом закрыты остальные админские экраны: `create` и `approvals` — под
      `PlannerAdminOnly` (вид апки), `admin` (Directories) остаётся под портальным `AdminOnly`.
      Корень `/task-planner` бригадира не отбивает, а редиректит на его My Tasks (`PlannerHome`).
- [x] **Гейт по виду апки** — решено 2026-08-04: видов ровно два, без вида в апку не пускаем.
      `TaskPlannerLayout` → `AccessDenied` с пояснением, что роль не назначена. Общий компонент
      `components/AccessDenied.tsx` (переиспользован из `AppAccessGuard`).

### 4.2 Tasks (`/task-planner`)

- [x] **У бригадира — свой экран `My Tasks`**, а не админский список с табами (решено
      2026-08-13, см. §5). Админский `/task-planner` бригадиру не показываем вовсе.
- [x] **Фильтр по своей бригаде** — держит RLS (`0007`), а не запрос: фильтр в UI обходится
      прямым запросом с anon-ключом, поэтому второго контура в коде нет намеренно.
- [x] **Скрыть админские действия**: «Send to AI», «Reset test …», удаление, drag-n-drop —
      живут на экране Tasks, а он бригадиру недоступен (пункт меню + роут).
- [x] **Табы статусов**: бригадиру их не показываем — он видит только `scheduled` своей
      бригады, разложенные по дням (Yesterday / Today / Tomorrow).
- [x] **Функционал бригадира** — получен от заказчика 2026-08-13 (прототип Lovable
      `crew-wise-ai`): отметка выполнения + заметки + фото. Модель — §5.

### 4.3 Create Task (`/task-planner/create`)

- [x] **Решено 2026-08-13: скрыт у бригадира целиком** (пункт меню + роут). Причина не только
      в UX: после `0007` вставка в `tp_tasks` разрешена лишь виду Planner Admin, так что экран
      у бригадира всё равно падал бы на сохранении. Если заказчик захочет «бригадир заводит
      себе задачу» — это отдельная политика INSERT со скоупом по своей бригаде.

### 4.4 Teams Availability (`/task-planner/availability`)

- [ ] `team_lead` — только своя бригада: убрать выбор чужих бригад, показывать свой календарь.
- [ ] Запись уже прикрыта на уровне БД политикой `tp_availability_own_team` (миграция `0006`),
      UI должен ей соответствовать, а не полагаться на неё одну.

### 4.5 Directories (`/task-planner/admin`)

- [ ] Только `admin` — и в меню (уже `adminOnly`), и роутом (см. 4.1).
- [ ] **Переписать вкладку Team.** Сейчас `fetchTeamMembers()` мержит `tp_profiles` +
      `tp_user_roles` + `tp_teams`, а колонка Role показывает роль из мёртвой `tp_user_roles`.
      По новой модели вкладка должна показывать: бригада → её email → **есть ли у неё вход в
      портал** (строка в `profiles`) и доступ к апке. `tp_user_roles`/`tp_profiles` из запроса убрать.

### 4.6 Данные и RLS

- [x] **`0006_tp_rls_lockdown.sql` применена** на боевой БД 2026-08-04 (SQL Editor,
      `pilxwhtkhysanpukaliu`). Утечка закрыта: анониму все 7 таблиц отдают `0 rows`.
- [x] **`tp_tasks` — скоуп по бригаде в RLS**: миграция **`0007_tp_task_execution.sql`**
      (ось исполнения + журнал + фото + RLS `tp_tasks`) — **применена на боевой 2026-08-13**.
- [ ] ⏳ **`0008_tp_planner_admin.sql` — применить на боевой.** Без неё вид Planner Admin,
      выданный маппингом ролей (не портальный админ), теряет запись в `tp_tasks` и справочники:
      `0007` завязал политики на портального `user_has_admin_role`, а модель апки шире.
- [ ] Проверить после применения: админ видит всё, бригадир — только своё, синки и n8n живы
      (они на `service_role`, RLS их не касается — но прогнать «Send to AI» ради уверенности).

**Что даёт `0007`** (детали — в шапке самого файла):
- `tp_tasks.execution_status` + backfill из legacy-флага `completed` (его держим в синхроне —
  на него могут смотреть n8n/старые сценарии);
- `tp_task_events` (append-only) и `tp_task_photos` + **приватный** бакет `tp-task-photos`
  (фото отдаются через `createSignedUrl`; публичность бакетов — принятый риск SEC-5, новое
  его не наследует);
- RPC `tp_complete_task` / `tp_approve_task` / `tp_rework_task` / `tp_add_task_note` /
  `tp_add_task_photo` — **единственный** способ поменять `execution_status`. Так сделано потому,
  что RLS ограничивает строки, а не колонки, а колоночные гранты вешаются на роль
  `authenticated` — то есть заодно урезали бы админа, который ходит под той же ролью;
- RLS `tp_tasks`: админ — всё, бригадир — **только чтение** задач своей бригады;
- `tp_can_approve_task()` — одна точка, где зашито «апрувит портальный админ». Когда придёт
  ответ от Павла (§7), правится тело этой функции, политики не трогаются.

> ⚠️ Бригадир без строки в `tp_teams` (вид `team_lead` выдан только маппингом роли — п.4 таблицы
> §2) не увидит **ни одной** задачи: `tp_my_team_id()` вернёт `null`. Это ожидаемо, но при
> раздаче доступов (4.7) email в `tp_teams` должен совпадать с рабочим Google-аккаунтом.

### 4.7 Выдача доступа (разовое, руками)

- [ ] Завести в **Portal Settings → Roles** роль «Team Lead» с единственным приложением Task Planner.
- [ ] Пригласить 9 бригадиров из `tp_teams` (все `@achgroupllc.com`) в **Portal Settings → Users**.
- [ ] Проверить под реальной ограниченной учёткой (не под админом) — это же закрывает
      висящий пункт «Тесты доступа» из `PORTAL.md` §6.

### 4.8 Чистка

- [ ] Edge-функция `set-team-password` больше не вызывается фронтом (проверяет `super_admin`
      по `tp_user_roles`, которая уходит). Оставлена на сервере — удалить при чистке.
- [ ] Таблицы `tp_user_roles` / `tp_profiles` после переписи 4.5 не используются — решить,
      дропать ли (в них могут быть исторические строки).

---

## 5. Исполнение задачи — модель (решено 2026-08-13)

Источник: прототип Lovable **`crew-wise-ai`** (экраны `My Tasks`, `Task Details`,
`Task Completion Details`), показан заказчиком 2026-08-13.

### 5.1 Две независимые оси

Планирование и исполнение — **разные оси, не одно поле**. `scheduled` и «выполнена» не
взаимоисключающи: задача остаётся запланированной на дату и при этом может быть выполнена.

| Ось | Поле | Значения |
|---|---|---|
| **Планирование** (как есть) | `tp_tasks.status` | `requested → proposed → scheduled` (+ `archived`, объявлен в типах, в UI не используется) |
| **Исполнение** (новое) | `tp_tasks.execution_status` | `pending → completed → approved`, ветка `rework → pending` |

Ось исполнения живёт **только у `status = 'scheduled'`**: пока расписание не утверждено,
исполнять нечего. Дефолт при переходе в `scheduled` — `pending`.

| `execution_status` | Что значит | Кто ставит |
|---|---|---|
| `pending` | назначена, бригадир ещё не отмечал | система (дефолт) |
| `completed` | бригадир нажал **Complete** (+ `completed_at`, `completed_by`) | `team_lead` |
| `approved` | подтверждено сверху, задача закрыта | апрувер (см. 5.4) |
| `rework` | вернули бригадиру с комментарием; из `rework` задача снова доступна к Complete → `pending` | апрувер |

**`in_progress` не вводим** (решено 2026-08-13): кнопки «Start» у бригадира нет, а статус без
явного действия будет врать. Если заказчик потом попросит «вижу, что бригада выехала» —
добавляем кнопку Start и статус вместе, одним куском.

### 5.2 История манипуляций — `tp_task_events` (append-only)

Кнопка **info** в `Task Details` открывает `Task Completion Details → Recent History`. Это
**не поля в задаче**, а отдельная журнальная таблица — только так в одну ленту лягут и отметка
бригадира, и комментарий апрувера, и возврат на доработку.

```
tp_task_events (append-only, никогда не UPDATE/DELETE)
  id, task_id, actor_id, actor_role ('admin' | 'team_lead'),
  event_type ('completed' | 'approved' | 'rework' | 'note' | 'photo' | 'status_change'),
  from_value, to_value,   -- для смен статуса
  comment,                -- текст (комментарий апрувера / заметка)
  created_at
```
Окно «Recent History (Last 3 changes)» = последние N строк по `task_id`, свежие сверху.
Notes и фото из модалки — тоже события (`note` / `photo`); вложения — `tp_task_photos`
(`task_id`, `event_id`, `path` в бакете, `created_at`).

### 5.3 Экраны

- **`My Tasks`** (бригадир, прототип) — дата + переключатель `Yesterday / Today / Tomorrow`,
  список задач **своей бригады** со `status = 'scheduled'` на выбранный день, кнопка `Complete`
  на карточке. Клик по карточке → `Task Details`: `info` (история), `Notes` + `Add Note`,
  `Photos` + `Upload Photos`, `Complete`.
- **Approval-вью** (апрувер) — на прототипе его нет, но без него `completed` некуда девать:
  список задач в `completed`, действия `Approve` и `Send to rework` + комментарий.
  Комментарий при `rework` — обязателен.

### 5.4 Кто апрувит

**Пока — портальный админ** (`admin`), это временное решение на время разработки.
Финальную модель уточняем у Павла — см. §7.

---

## 6. Матрица прав

| Экран / действие | `admin` | `team_lead` |
|---|---|---|
| Tasks (админский список, табы) | все задачи | ❌ не показываем |
| My Tasks (Yesterday/Today/Tomorrow) | ✅ (для проверки) | только своя бригада, только `scheduled` |
| Tasks — Send to AI / Reset / удаление / dnd | ✅ | ❌ |
| Complete + Notes + Photos на своей задаче | ✅ | ✅ |
| Approve / Send to rework | ✅ (пока только он, §5.4) | ❌ |
| История задачи (info) | ✅ | ✅ (свои задачи) |
| Create Task | ✅ | ❓ решить (4.3) |
| Teams Availability | все бригады | только своя |
| Directories | ✅ | ❌ |
| App Settings | ✅ | ❌ |

---

## 7. Открытые вопросы

### Уточнить у Павла

- [ ] 🔴 **Кто апрувит выполненную задачу.** Варианты: (а) PM из поля `project_manager` самой
      задачи, (б) любой Planner Admin, (в) отдельная третья роль «Approver». От ответа зависят
      RLS (`tp_tasks` на запись `execution_status`) и число видов в апке — сейчас их ровно два,
      вариант (а)/(в) добавит третий. **Пока делаем: апрувит портальный админ** (§5.4).
- [ ] Что происходит с задачей после `approved` — уходит из списков? Пишется ли что-то обратно
      в Airtable / Buildertrend, или закрытие живёт только в планировщике.
- [ ] Может ли бригадир отметить Complete **задним числом** (вкладка Yesterday) и есть ли лимит
      по времени. Сейчас в прототипе Yesterday доступен, ограничений нет.
- [ ] Обязательны ли фото/заметка при Complete, или можно закрыть пустым.
- [ ] Нужно ли уведомление апруверу о новых `completed` (Slack — рассыльщик уже есть).

### Прочее

- [x] **Функционал бригадира** — получен 2026-08-13 (прототип `crew-wise-ai`), модель в §5.
- [ ] Карточка апки на «My Applications» видна по доступу к приложению, а вид может быть не
      назначен — такой юзер кликнет и упрётся в Access denied. Скрывать ли карточку (тогда
      фильтр карточек должен учитывать роли апки, а не только `applications`).
- [ ] Бригады в `tp_teams` — это *бригада* (юрлицо/команда), а логинится *человек*. Если у
      одной бригады несколько человек с разными email, модель «один email = одна бригада»
      их не покроет: понадобится таблица связи «юзер → бригада». Сейчас данные это допускают
      (у каждой из 9 бригад ровно один email), но уточнить у заказчика стоит.
