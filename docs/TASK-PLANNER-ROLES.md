# Task Planner — роли и разграничение доступа (план работ)

> Создано **2026-08-04**. Задача: внутри Task Planner два вида пользователей — **админы
> планировщика** (создают задачи, гоняют AI-планирование) и **бригадиры/PM**, которым
> нужен сильно урезанный вид. Тут — принятая модель, что уже сделано и **полный список
> UI-работ**. Функционал бригадира заказчик даёт отдельно — под него оставлены слоты.
> Смежное: [`TASK-PLANNER.md`](TASK-PLANNER.md) (бизнес-логика, n8n), [`PORTAL.md`](PORTAL.md) (платформа).

---

## 1. Принятые решения (2026-08-04)

| # | Решение | Почему |
|---|---|---|
| **Р-1** | **Вход только через Google OAuth**, как у всех остальных | Все 9 строк `tp_teams` имеют email на `@achgroupllc.com` — это подтверждённый Workspace-домен портала (`PORTAL.md` §8). Отдельный вход бригадирам не нужен |
| **Р-2** | **Роль внутри апки не хранится**, а выводится из портальных данных | Правило 3 платформы: контур допуска — портальный. Апка не заводит свой |
| **Р-3** | `tp_user_roles` + кнопка **Set password** — **выпилены из UI** | Наследие Lovable-версии TP со своим логином. Кнопка вела в тупик: создавала учётку email+пароль, а на экране входа портала формы пароля нет вообще — войти такой учёткой невозможно |
| **Р-4** | **RLS на `tp_*` закрывается** (миграция `0006`) | Найдена утечка: часть таблиц читалась анонимом без логина (см. `PORTAL.md`, SEC-7) |

---

## 2. Модель ролей

**Где настраивается:** `/settings/task-planner` → вкладка **Roles**. Админ отмечает, какие
**портальные роли** дают вид «Planner Admin», а какие — «Team Lead». Маппинг лежит в
`app_settings` под ключами `roles_admin` / `roles_team_lead` (jsonb-массив `role_id`).
Сами роли и пользователи заводятся только в Portal Settings — апка своего реестра не имеет
(правило 3). Слоты ролей объявлены в `appRegistry.ts` → `AppConfig.appRoles`, поэтому
вкладка Roles автоматически появится у любой другой апки, которой понадобятся свои виды.

Роль вычисляется на лету, нигде не хранится (`useTaskPlannerRole()`), по порядку:

| # | Условие | Роль | Кто это |
|---|---|---|---|
| 1 | админ портала (`useAuth().isAdmin`) | **`admin`** | Влад, Павел — всегда, независимо от настройки |
| 2 | роль юзера ∈ `roles_admin` (вкладка Roles) | **`admin`** | планировщик-диспетчер без прав админа портала |
| 3 | email юзера совпал с `tp_teams.email` | **`team_lead`** | бригадир/PM своей бригады |
| 4 | роль юзера ∈ `roles_team_lead` | **`team_lead`** | бригадир, которого нет в `tp_teams` (без привязки к бригаде) |
| 5 | остальные | **`none`** | **в апку не пускаем** — Access denied |

**Видов ровно два.** Доступ к апке в портале (`applications`) сам по себе вида не даёт: если
пользователю не назначен ни Planner Admin, ни Team Lead, `TaskPlannerLayout` показывает
Access denied вместо любого экрана планировщика. Гейт стоит на обёртке всех роутов
`/task-planner/*`, поэтому прямая ссылка его не обходит.

Реализация — два зеркальных слоя, гейт обязан быть на обоих:

- **UI**: `pages/task-planner/useTaskPlannerRole.ts` → `{ role, teamId, teamName }`.
  Ищет бригаду через `fetchTeamByEmail()` (узкий запрос по `tp_teams`, `ilike` = регистронезависимое равенство).
- **БД**: SQL-функция `public.tp_my_team_id()` (миграция `0006`, `security definer`) — тем же
  правилом (email ↔ `tp_teams.email`) скоупит RLS-политики.

Чтобы бригадир вообще попал в апку, ему нужно (обычным портальным путём, **не** внутри TP):
1. приглашение в **Portal Settings → Users** на рабочий Google-аккаунт;
2. роль, у которой в доступах есть Task Planner (`role_applications`);
3. эта же роль — отмечена в **App Settings → Roles → Team Lead** (либо его email есть в `tp_teams`).

---

## 3. Что уже сделано (2026-08-04)

- [x] **Вкладка Roles в App Settings** (`/settings/task-planner`) — маппинг «внутренняя роль
      апки → портальные роли», сохраняется в `app_settings`. Механизм общий: слоты объявляются
      в `appRegistry` (`appRoles`), вкладка появляется сама. Сервис — `getAppRoleIds()`.
- [x] `useTaskPlannerRole()` — хук роли (портальный админ / маппинг ролей / бригада по email).
- [x] `fetchTeamByEmail()` в `services/task-planner/data.ts`.
- [x] `tp_my_team_id()` + закрытие RLS — миграция `0006_tp_rls_lockdown.sql`, **применена на боевой** 2026-08-04.
- [x] Убраны кнопка **Set password**, модалка и сервис `setTeamPassword`; на вкладке Team —
      пояснение, что доступ выдаётся в Portal Settings.
- [x] Меню апки переименовано: **Admin → Directories** (не путать с админкой портала).
- [x] Портальное **User Management → Portal Settings** (`/portal-settings`, старый `/users` — редирект).

---

## 4. Что надо сделать по UI — полный список

### 4.1 Оболочка и навигация

- [ ] **Пункты меню по роли апки.** Сейчас `AppNavItem` умеет только `adminOnly` (портальный
      админ). Нужен механизм «пункт виден роли X внутри апки»: апка публикует свою роль в
      контекст (`AppRoleProvider` в `TaskPlannerLayout`), `Layout.tsx` фильтрует по нему.
      Делать обобщённо — тем же механизмом воспользуются HR-Checklists и Production.
- [x] **Роут-гейт `/task-planner/admin`** — 🔴 был открыт по прямой ссылке любому, у кого есть
      доступ к Task Planner (роут без `AdminOnly`; `AppAccessGuard` различает только апки
      целиком, а `TaskPlannerLayout` ничего не проверял). Закрыт `AdminOnly` 2026-08-04.
- [ ] Тем же гейтом закрыть остальные экраны, которые окажутся админскими по матрице §5
      (по роли апки, а не только по портальному `isAdmin`).
- [x] **Гейт по виду апки** — решено 2026-08-04: видов ровно два, без вида в апку не пускаем.
      `TaskPlannerLayout` → `AccessDenied` с пояснением, что роль не назначена. Общий компонент
      `components/AccessDenied.tsx` (переиспользован из `AppAccessGuard`).

### 4.2 Tasks (`/task-planner`)

- [ ] **Фильтр по своей бригаде**: `team_lead` видит только задачи с `team_id = его бригада`.
      Фильтровать в запросе (`fetchTasks`), не в рендере.
- [ ] **Скрыть админские действия**: «Send to AI», «Reset test …», удаление задачи (иконка
      корзины на каждой карточке), drag-n-drop переупорядочивания.
- [ ] **Табы статусов**: решить, какие видит бригадир. Логично оставить только `Scheduled`
      (утверждённое расписание) и спрятать `Proposed`/`Requested` — это кухня планирования.
- [ ] **Слот под функционал бригадира** — что он делает с задачей (отметка выполнения,
      комментарий, фото?). Ждём от заказчика.

### 4.3 Create Task (`/task-planner/create`)

- [ ] Решить: скрыть у бригадира целиком **или** оставить с жёстко подставленной своей
      бригадой и без выбора чужих. Сейчас пункт виден всем.

### 4.4 Teams Availability (`/task-planner/availability`)

- [ ] `team_lead` — только своя бригада: убрать выбор чужих бригад, показывать свой календарь.
- [ ] Запись уже прикрыта на уровне БД политикой `tp_availability_own_team` (миграция `0006`),
      UI должен ей соответствовать, а не полагаться на неё одну.

### 4.5 Directories (`/task-planner/admin`)

- [ ] Только `admin` — и в меню (уже `adminOnly`), и роутом (см. 4.1).
- [ ] **Переписать вкладку Team.** Сейчас `fetchTeamMembers()` мержит `tp_profiles` +
      `tp_user_roles` + `tp_teams`, а колонка Role показывает роль из мёртвой `tp_user_roles`.
      По новой модели вкладка должна показывать: бригада → её email → **есть ли у неё вход в
      портал** (строка в `profiles`) и доступ к апке. `tp_user_roles`/`tp_profiles` из запроса убрать.

### 4.6 Данные и RLS

- [x] **`0006_tp_rls_lockdown.sql` применена** на боевой БД 2026-08-04 (SQL Editor,
      `pilxwhtkhysanpukaliu`). Утечка закрыта: анониму все 7 таблиц отдают `0 rows`.
- [ ] **`tp_tasks` — скоуп по бригаде в RLS**: `team_id = tp_my_team_id()` для не-админа.
      Без этого фильтр в UI обходится прямым запросом с anon-ключом.
- [ ] Проверить после применения: админ видит всё, бригадир — только своё, синки и n8n живы
      (они на `service_role`, RLS их не касается — но прогнать «Send to AI» ради уверенности).

### 4.7 Выдача доступа (разовое, руками)

- [ ] Завести в **Portal Settings → Roles** роль «Team Lead» с единственным приложением Task Planner.
- [ ] Пригласить 9 бригадиров из `tp_teams` (все `@achgroupllc.com`) в **Portal Settings → Users**.
- [ ] Проверить под реальной ограниченной учёткой (не под админом) — это же закрывает
      висящий пункт «Тесты доступа» из `PORTAL.md` §6.

### 4.8 Чистка

- [ ] Edge-функция `set-team-password` больше не вызывается фронтом (проверяет `super_admin`
      по `tp_user_roles`, которая уходит). Оставлена на сервере — удалить при чистке.
- [ ] Таблицы `tp_user_roles` / `tp_profiles` после переписи 4.5 не используются — решить,
      дропать ли (в них могут быть исторические строки).

---

## 5. Матрица прав — черновик

Заполняется после того, как заказчик даст функционал бригадира. Текущее предположение:

| Экран / действие | `admin` | `team_lead` |
|---|---|---|
| Tasks — список | все задачи | только своя бригада |
| Tasks — Send to AI / Reset / удаление | ✅ | ❌ |
| Tasks — действия над своей задачей | ✅ | **ждём функционал** |
| Create Task | ✅ | ❓ решить (4.3) |
| Teams Availability | все бригады | только своя |
| Directories | ✅ | ❌ |
| App Settings | ✅ | ❌ |

---

## 6. Открытые вопросы

- [ ] **Функционал бригадира** — что именно он делает в Tasks. Даёт заказчик.
- [ ] Карточка апки на «My Applications» видна по доступу к приложению, а вид может быть не
      назначен — такой юзер кликнет и упрётся в Access denied. Скрывать ли карточку (тогда
      фильтр карточек должен учитывать роли апки, а не только `applications`).
- [ ] Бригады в `tp_teams` — это *бригада* (юрлицо/команда), а логинится *человек*. Если у
      одной бригады несколько человек с разными email, модель «один email = одна бригада»
      их не покроет: понадобится таблица связи «юзер → бригада». Сейчас данные это допускают
      (у каждой из 9 бригад ровно один email), но уточнить у заказчика стоит.
