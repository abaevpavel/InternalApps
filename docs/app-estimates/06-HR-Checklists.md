# 06-HR-Checklists — план разработки и estimate

> Модель оценки: **vibe-coding (билдит Claude), в ЧАСАХ.** Код генерируется быстро —
> время уходит на итерации и проверку, не на набор. Оценка на честный rebuild до
> прод-стандарта (React Query, RLS с самого начала, типобезопасность, тесты ролей, деплой).
> Стек как в оригинале: React + Vite + TS + Supabase + edge functions.

**Источник истины по поведению:** разбор Lovable (БД-схема, RLS, edge functions, Make/cron,
объёмы, current-state). См. краткое описание ниже.

---

## Что это
HR назначает сотрудникам чеклисты из библиотеки, отмечает задачи в 3 состояниях (✓/×/N/A),
прикрепляет фото/заметки, экспортирует PDF. Иерархические задачи (`parent_id`),
drag-and-drop редактор шаблонов, AI-импорт чеклиста из картинки, синк сотрудников/вендоров
в Airtable через Make.com по расписанию (pg_cron).

**Сложность: средне-высокая.** Трудоёмкость даёт не CRUD, а: иерархический dnd-редактор
с undo, каскадная пропагация состояний, 3-state, PDF, AI-импорт.

## Простыми словами: кто что делает
- **HR-менеджер** — выбирает сотрудника, назначает ему чеклисты, отмечает галочки (✓ сделано /
  ✗ нет / N/A не нужно), цепляет фото и заметки, жмёт «скачать PDF».
- **Приложение (в браузере)** — показывает чеклист деревом (подзадачи под задачами),
  само считает прогресс, складывает все отметки и фото в базу, собирает PDF.
- **База (Supabase)** — хранит библиотеку чеклистов, сотрудников, кто что отметил, фото.
- **ИИ (через edge function)** — если HR загрузил картинку чеклиста, ИИ сам распознаёт пункты
  и строит из них дерево задач (не вбивать руками).
- **«Будильник» базы (cron) + Make** — отдельная мелкая функция: 2 раза в день сами обновляют
  контакты сотрудников/вендоров в Airtable (то же, что делает приложение HR-Sync Airtable).
> Главное: вся работа с чеклистами происходит **внутри приложения и его базы**; наружу
> (Make/Airtable) уходит только вспомогательный синк контактов.

---

## Scope (что строим)
- **2 экрана:** `/checklists` (сотрудники + назначение + отметка + заметки/фото + PDF),
  `/checklist/:id` (редактор шаблона: dnd-дерево, inline-edit, indent/unindent, undo, дубль/удаление, AI-импорт).
- **БД:** ~8 таблиц (`checklists`, `checklist_items` self-FK, `employees`,
  `employee_checklists`, `employee_checklist_progress`, `employee_phase_preferences`,
  `employee_types`, `checklist_photos`) + UNIQUE-констрейнты + RLS.
- **Storage:** 2 бакета, загрузка фото с JPEG-компрессией ~200 KB, галереи.
- **Edge functions:** `extract-checklist-from-image` (AI function calling, парс дерева),
  `manage-sync-schedules` (pg_cron RPC: schedule/unschedule).
- **Интеграции:** 2 Make.com webhook (trigger-only POST `{action}`), 4 cron-джобы.
  Внутренняя логика Airtable-синка — **в сценариях Make, вне scope приложения.**
- **Auth/роли:** Admin + HR Manager, модель `applications → role_applications → user_roles`, RLS.

---

## Перенос «как есть» (план сессиями, ≈11 ч)
Воспроизводим текущее прод-поведение 1:1, чистым кодом. **Без** доделок/улучшений (они — в «Дырах»).
Часы — на чистую вайб-сборку, без «прод-стандарт»-наценки.

| # | Сессия (deliverable) | Состав | Часы |
|---|----------------------|--------|------|
| 1 | **Каркас + БД** | Vite/TS/Tailwind/shadcn, supabase-клиент, auth, роутинг; схема 8 таблиц + миграции + констрейнты | 1.5 |
| 2 | **RLS + access-модель** | политики под Admin/HR Manager, `applications→role_applications→user_roles`, тест ролей | 1.0 |
| 3 | **Экран `/checklists`** | CRUD сотрудников, назначение, 3-state, прогресс (leaf-only), заметки | 2.0 |
| 4 | **Редактор `/checklist/:id`** | dnd-дерево, inline-edit, indent/unindent, undo, дубль/удаление ← **главный риск** | 2.0 |
| 5 | **Каскадная логика** | пропагация N/A/✓ вверх-вниз, answer_options, buildTaskTree | 0.75 |
| 6 | **Фото + PDF** | upload+компрессия+галереи, @react-pdf экспорт | 1.25 |
| 7 | **AI-импорт** | edge function (function calling) + UI | 1.0 |
| 8 | **Sync: cron + UI + webhooks** | manage-sync-schedules + pg_cron + HR Sync UI + 2 Make-POST (как есть) | 0.75 |
| 9 | **QA + деплой** | edge-кейсы пропагации, прод-деплой | 0.75 |
| | **Итого перенос** | | **≈ 11 ч** |

**Вилка переноса:** пол ~9 · реалистично ~11 · с трением ~14 (трение почти весь — в dnd-редакторе).

> **Единственный капризный блок — редактор (dnd-kit + иерархия + sort_order + undo).** Он же
> главный swing: гладко → ~10, упрётся → ~12.
> **Рычаг до ~10 ч:** упростить undo на v1 (1–2 шага или без него) — снимает ~1 ч и основной риск.
> Прочее трение (не сжимается): тюнинг AI-парсинга дерева, RLS под 2 роли, координация с Make (внешнее).

---

## Дыры / исправления (отдельно, поверх переноса)
Что в оригинале сломано/хрупко/долг. Каждая — со своим временем. Делаются опционально.

| # | Дыра | Суть / чем грозит | Фикс | Prio | Часы |
|---|------|-------------------|------|------|------|
| F1 | **Cron DST** | ET→UTC жёстко +5, летом синк сдвинут на час | DST-корректная конвертация + тест лето/зима | P1 | 0.5 |
| F2 | **Webhook URLs в клиенте** | 2 Make-URL зашиты в bundle → публичны | вынести в env/edge-proxy | P2 | 0.5 |
| F3 | **Legacy hardcoded фазы** | `phase1/2a/2b/3` массивы рядом с динамикой → дубль в `handleTaskToggle` | полностью data-driven, выпилить legacy-ветку | P2 | 1.0 |
| F4 | **Мёртвый/двойной код** | `checklist_photos` (0 строк) не используется; `employee_types` vs enum — две типизации | убрать неиспользуемое, оставить один механизм | P3 | 0.5 |
| F5 | **`manage-sync-schedules` GET** | не подгружает сохранённые времена → UI всегда показывает зашитые 11:00/17:00 | реализовать `get_sync_schedules` + подтянуть на фронт | P2 | 0.5 |
| | **Итого исправления** | | | | **≈ 3.0 ч** |

> Базовый набор (F1+F5 — корректность синка) ~1 ч; полный с чисткой долга ~2.5–3 ч.
> Примечание: правильную RLS закладываем уже в переносе (сессия 2), отдельной дырой не считаем.

---

## Итог
**Перенос ~11 ч + исправления ~2.5–3 ч = ~13.5 ч.** Цель переноса 10 ч достижима (упростить undo).
Внешний движок (Make/Airtable-синк) — вне часов.

---

## Контракты и факты (из разбора Lovable)
**Make.com webhooks (direct POST, без auth):**
- Employees → `https://hook.us1.make.com/083dd6fp7plulvfvdrb2wagximfp29g2`, payload `{"action":"sync_employees"[,"scheduled":true]}`
- Vendors → `https://hook.us1.make.com/c7njtsv68o6mc563exam599j7di9ejhs`, payload `{"action":"sync_vendors"[,"scheduled":true]}`
- Триггеры: данных не несут, только `action`. Данные тянет сценарий внутри Make.

**Cron (pg_cron):** sync-employees 11:00/17:00 ET, sync-vendors 11:10/17:10 ET (в UTC, ⚠ DST не учтён, +5 жёстко).

**Airtable:** base `appiScywNMqBk3x9e`; таблицы/маппинг — UNKNOWN (логика в Make). Сущности: Employee Contacts, Key Vendor Contacts. Прямых вызовов Airtable API в коде нет.

**Secrets:** собственных у приложения нет. Frontend — `VITE_SUPABASE_*`. Webhook URLs захардкожены в коде (вынести).

**Объёмы:** checklists 11, checklist_items 490, employees 8, employee_checklists 13,
employee_checklist_progress 609, employee_phase_preferences 1, employee_types 3,
checklist_photos 0. Топ-чеклист: 98 items / depth 3. Нагрузочных проблем нет.

**Current-state:** работают CRUD/редактор/назначение/3-state/пропагация/фото/PDF/AI-импорт/синк/cron/RLS.
Частично: legacy-фазы, `employee_phase_preferences` (1 строка), двойная типизация, DST.
Сломано/неиспользуется: `checklist_photos` (0 строк), webhook URLs в клиенте,
`manage-sync-schedules` GET не подгружает сохранённые времена (показывает зашитые).

---

## Допущения и границы
- **Вне scope:** внутренняя логика Make.com-сценариев и маппинг полей Airtable — существующие
  сценарии, оцениваются отдельно.
- AI-импорт — на провайдере уровня Gemini 2.5 Flash / аналог (function calling).
- Оценка на **faithful rebuild**; новые фичи сверх оригинала — отдельно.
