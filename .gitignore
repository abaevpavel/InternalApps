node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
*.tsbuildinfo

# SSH keys / secrets
*.pem
*.key

# Data-copy migrations with real business data (apply locally, never commit)
*.local.sql

# Supabase CLI служебный кэш
**/supabase/.temp/
